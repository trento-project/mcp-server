# Copyright 2025 SUSE LLC
# SPDX-License-Identifier: Apache-2.0

{{ if .Values.mcpo.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "trento-ai-companion.fullname" . }}-mcpo
  namespace: {{ .Release.Namespace }}
  labels:
    app: mcpo
  {{- include "trento-ai-companion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.mcpo.type }}
  selector:
    app: mcpo
    {{- include "trento-ai-companion.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.mcpo.ports | toYaml | nindent 2 }}
{{- end }}
