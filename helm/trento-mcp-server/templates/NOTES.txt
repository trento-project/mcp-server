Set your preferred OpenAPI file in the ConfigMap by running the following command:

    kubectl --namespace {{ .Release.Namespace }} create configmap {{ include "trento-ai-companion.fullname" . }}-mcp-server --from-file ./api/openapi.json -o yaml --dry-run=client | kubectl apply -f -

If needed, you can force the restart of the Pods by running:

    kubectl --namespace {{ .Release.Namespace }} rollout restart deployment {{ include "trento-ai-companion.fullname" . }}-mcp-server
{{- if .Values.mcpo.enabled }}
    kubectl --namespace {{ .Release.Namespace }} rollout restart deployment {{ include "trento-ai-companion.fullname" . }}-mcpo
{{- end }}
