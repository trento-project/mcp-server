# Copyright 2025 SUSE LLC
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.mcpServerTrento.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "trento-ai-companion.fullname" . }}-mcp-server
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "trento-ai-companion.labels" . | nindent 4 }}
  annotations:
  {{- with .Values.mcpServerTrento.ingress.annotations -}}
    {{ toYaml . | nindent 4 }}
  {{- end -}}
    {{- if eq .Values.mcpServerTrento.ingress.ingressClassName "traefik" }}
    kubernetes.io/ingress.class: traefik
    # This middleware strips the prefix from the request path
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Release.Namespace }}-{{ include "trento-ai-companion.fullname" . }}-strip-prefix@kubernetescrd
    {{- end }}
spec:
  ingressClassName: traefik
  rules:
{{- range .Values.mcpServerTrento.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path }}
        pathType: {{ .pathType }}
        backend:
          service:
            name: {{ include "trento-ai-companion.fullname" $ }}-mcp-server
            port:
              name: http
      {{- end }}
{{- end }}
  tls:
  - hosts:
  {{- range .Values.mcpServerTrento.ingress.hosts }}
    - {{ .host }}
{{- end }}
{{- end }}
